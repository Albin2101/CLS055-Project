# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Android/Gradle build outputs (ignore all build directories under android)
/android/**/build/
/android/**/.gradle/
/android/.gradle/
/android/app/**/build/
/android/local.properties

# Gradle caches and wrapper
/.gradle/
/.gradle


# Firebase generated options (keep out of VCS)
/lib/firebase_options.dart
# Optional: ignore platform-specific config files if you don't want them in repo
/android/app/google-services.json
/ios/Runner/GoogleService-Info.plist

# Other possible platform config files that may contain API keys
# Web
/web/firebase-messaging-sw.js
/web/firebase-config.js
/web/firebase.js

# macOS
/macos/Runner/GoogleService-Info.plist

# Windows
/windows/runner/GoogleService-Info.plist

# Linux
/linux/runner/GoogleService-Info.plist

# Desktop (other common names)
**/firebase_options.dart

# Environment files that sometimes hold keys
.env
.env.*
env.local

# CI or scripts that might accidentally include keys
.github/workflows/**
ci/**
